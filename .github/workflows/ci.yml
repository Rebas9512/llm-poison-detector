name: LLM Poison Detector CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: py${{ matrix.python-version }} / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      # Install CPU-only torch first to avoid the multi-GB CUDA bundle
      - name: Install CPU torch
        run: pip install "torch>=2.4.0" --index-url https://download.pytorch.org/whl/cpu

      - name: Install project + dev dependencies
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Build distribution wheel
        run: python -m pip wheel . --no-deps --no-build-isolation -w dist

      # ── Setup script syntax checks ────────────────────────────────────────
      - name: setup.sh syntax (Unix)
        if: runner.os != 'Windows'
        run: bash -n setup.sh

      - name: install.sh syntax (Unix)
        if: runner.os != 'Windows'
        run: bash -n install.sh

      - name: setup.sh executable bit
        if: runner.os != 'Windows'
        run: |
          mode=$(git ls-files -s setup.sh | awk '{print $1}')
          if [ "$mode" != "100755" ]; then
            echo "ERROR: setup.sh is not executable in git index (mode=$mode)"
            exit 1
          fi
          echo "setup.sh mode: $mode  OK"

      - name: install.sh executable bit
        if: runner.os != 'Windows'
        run: |
          mode=$(git ls-files -s install.sh | awk '{print $1}')
          if [ "$mode" != "100755" ]; then
            echo "ERROR: install.sh is not executable in git index (mode=$mode)"
            exit 1
          fi
          echo "install.sh mode: $mode  OK"

      - name: setup.ps1 syntax (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $errors = $null
          $null = [System.Management.Automation.Language.Parser]::ParseFile(
            "$PWD/setup.ps1", [ref]$null, [ref]$errors
          )
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }
          Write-Host "setup.ps1 syntax OK"

      - name: install.ps1 syntax (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $errors = $null
          $null = [System.Management.Automation.Language.Parser]::ParseFile(
            "$PWD/install.ps1", [ref]$null, [ref]$errors
          )
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }
          Write-Host "install.ps1 syntax OK"

      # ── Python test suite ─────────────────────────────────────────────────
      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
